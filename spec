- Users management
- Products, cart, orders management, filters, search(elastic), pagination
  - services:
    - products - (shopify storefront api)
    - search - (algolia)
- Payment management(stripe)
- Onboarding(survey) management
- Booking management?

Admin UI project with REST api interface compatible with any back-end